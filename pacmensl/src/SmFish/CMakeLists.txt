add_library(SMFISH_OBJ OBJECT SmFishSnapshot.h SmFishSnapshot.cpp)

list(APPEND PACMENSL_OBJECTS "$<TARGET_OBJECTS:SMFISH_OBJ>")
set(PACMENSL_OBJECTS ${PACMENSL_OBJECTS} PARENT_SCOPE)

install(FILES SmFishSnapshot.h DESTINATION include/PACMENSL/)
